# Page: Auth

> Overrides `MASTER.md` for login / signup / password reset.

## Pattern
Split layout: brand panel (left/desktop) + form panel (right). Mobile: form first or stacked.

## Layout
- Brand panel may reuse landing hero background / particles (desktop)  
- Form: themed inputs, primary submit, ghost secondary links  
- Theme toggle on form panel

## Motion
ALLOW: soft In View / Text Effect on brand copy only  
BLOCK: heavy particles + Motion combo on mobile; skip live bg iframes &lt;768px

## CTA
Primary button = Log in / Sign up (black text on light theme if needed for contrast — match existing gallery auth rules).
